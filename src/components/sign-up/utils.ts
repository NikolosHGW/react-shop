export const INITIAL_STATE = {
  displayName: '',
  email: '',
  password: '',
  confirmPassword: '',
};

export interface Props {};

export interface State {
  displayName?: string,
  email?: string,
  password?: string,
  confirmPassword?: string,
};
